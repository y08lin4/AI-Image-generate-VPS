# 服务端监听端口
PORT=8787

# 访问密码（用户进入页面和调用受保护 API 时使用）
ACCESS_PASSWORD=change-me

# 管理员密码（用于管理员面板）
ADMIN_PASSWORD=change-me

# 是否允许上游 API 使用 HTTP（默认 true）
ALLOW_HTTP_API=true

# 是否允许上游 API 使用 localhost/内网地址（默认 false）
ALLOW_PRIVATE_HOSTS=false

# SQLite 数据库文件路径
DB_PATH=./data/app.db
